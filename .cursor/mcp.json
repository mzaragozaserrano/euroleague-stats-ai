{
  "mcpServers": {
    "text-to-sql": {
      "command": "poetry",
      "args": ["run", "python", "-m", "app.mcp_server"],
      "cwd": "${workspaceFolder}/backend",
      "env": {
        "PYTHONUNBUFFERED": "1"
      }
    }
  }
}
